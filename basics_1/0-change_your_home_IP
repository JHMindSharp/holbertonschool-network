#!/usr/bin/env bash
# This script modifies /etc/hosts to change the localhost IP and set facebook.com to 8.8.8.8

sudo sed -i 's/127.0.0.1/127.0.0.2/' /etc/hosts
echo "8.8.8.8 facebook.com" | sudo tee -a /etc/hosts > /dev/null
